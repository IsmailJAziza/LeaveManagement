@model LeaveTypeCreateVM

@{
    ViewData["Title"] = "Create Leave Type";
}

<h2>Create Leave Type</h2>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label> 
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <label asp-for="NumbersOfDays" class="control-label"></label>
                <input asp-for="NumbersOfDays" class="form-control" />
                <span asp-validation-for="NumbersOfDays" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group m-2">
                <input type="submit" value="Create"class="btn btn-success" />
                <a class="btn btn-primary" asp-action="Index">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
